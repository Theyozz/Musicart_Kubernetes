apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: musicart
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: musicart-frontend:local
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
